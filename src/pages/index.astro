---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { PAGE_INDEX_TITLE, SITE_DESCRIPTION, SITE_TITLE } from '../consts';
const pageTitle = `${PAGE_INDEX_TITLE} — ${SITE_TITLE}`;
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={pageTitle} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<h1>你好，欢迎。</h1>
			<p>
				这个站点用来沉淀我在编程学习与实践中写下的笔记。文章以
				<code>Markdown</code> / <code>MDX</code> 存放在
				<code>src/content/blog/</code>，通过 Astro 在构建时生成静态页面，便于长期保存、版本管理与部署。
			</p>
			<p>你可以从这些地方入手：</p>
			<ul>
				<li>在 <code>src/consts.ts</code> 中修改站名、简介、作者与 GitHub 地址</li>
				<li>在 <code>src/content/blog/</code> 里新增 <code>.md</code> 或 <code>.mdx</code> 文章（含 frontmatter）</li>
				<li>在 <code>src/pages/about.astro</code> 中写你的个人介绍</li>
				<li>上线前在 <code>astro.config.mjs</code> 中把 <code>site</code> 设为你的域名</li>
			</ul>
			<p>本地开发：<code>npm run dev</code>；生产构建：<code>npm run build</code>。</p>
		</main>
		<Footer />
	</body>
</html>
